import AppProvider, { AppContext } from './Providers/appProvider';

// Export individual components separately
// This way we avoid having unecessary index.js files
export {
    AppProvider,
    AppContext
};
